{
  "name": "elitepcparts",
  "version": "1.0.0",
  "private": true,
  "description": "Elite PC Parts static storefront",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && live-server dist --port=5173",
    "preview": "live-server dist --port=4173"
  },
  "dependencies": {
    "firebase": "^10.13.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "firebase-admin": "^14.1.0",
    "live-server": "^1.2.2"
  }
}
